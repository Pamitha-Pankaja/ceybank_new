package com.example.ceybank.repositories;

import com.example.ceybank.models.GoodRequisition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GoodRequisitionRepository extends JpaRepository<GoodRequisition, Long> {
}
